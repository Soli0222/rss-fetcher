apiVersion: v1
kind: Service
metadata:
  name: {{ include "rss-fetcher.fullname" . }}
  labels:
    {{- include "rss-fetcher.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "rss-fetcher.selectorLabels" . | nindent 4 }}
